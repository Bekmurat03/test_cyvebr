version: '3.8'

services:
  ctf-platform:
    build: .
    ports:
      - "5000:5000"
    environment:
      - SECRET_KEY=ctf_super_secret_key_2024
      - FLASK_ENV=production
    restart: unless-stopped
